apiVersion: v2
actions:
  - name: "Valdiate Results"
    events:
      - name: "sh.keptn.event.validate.triggered"
    tasks:
      - name: "Validate Results"
        image: "alpine"
        cmd:
          - echo
        args:
          - 'I'm validating the results'