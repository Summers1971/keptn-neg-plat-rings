apiVersion: v2
actions:
  - name: "Run Tests"
    events:
      - name: "sh.keptn.event.test.triggered"
    tasks:
      - name: "Run Tests"
        image: "alpine"
        cmd:
          - echo
        args:
          - 'I'm running the tests for entry validation'